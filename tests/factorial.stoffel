# Factorial sample that returns 120 for factorial of 5
# Uses an inclusive range 1..n and multiplies into an accumulator.

proc main() =
  let n = 5
  let acc = 1

  for i in 1 .. n:
    acc = acc * i

  return acc
