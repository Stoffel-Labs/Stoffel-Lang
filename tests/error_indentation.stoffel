# Indentation Errors

let x = 10

# Inconsistent dedentation
if x > 5:
  let y = 20
 let z = 30 # Error: Dedented too far or inconsistently

# Expected indentation
if x < 100:
  let a = 1 # Error: Expected an indented block

# Mixed tabs and spaces (Lexer should catch this)
if x == 10:
    let tab_indent = 1 # Using a tab character (will cause lexer error)
    let space_indent = 2 # Using spaces

# Unexpected indentation
let top_level = 1
  let bad_indent = 2 # Error: Unexpected indent at top level
